# Product Memo: 3As 智能面试官

## 1. 目标用户与核心痛点

### 目标用户

计算机专业本科生，正在准备保研夏令营/推免复试面试的学生。典型画像：大三下或大四上，已有科研/项目经历，需要在短时间内高效准备面试表达。

### 用户访谈发现

通过与多位保研成功/失败的同学交流，发现以下真实场景和痛点：

- **缺乏模拟对象**：身边同学水平参差不齐，很难找到能模拟教授追问风格的练习伙伴。很多人只能对着镜子自言自语，或者在面试当天才第一次面对追问。
- **不知道会被问什么**：面试官的追问往往基于你的回答深入，但学生很难预判哪些点会被挖。准备时容易"面面俱到但都不深"。
- **无法获得即时反馈**：即使找同学模拟，反馈也往往停留在"还行""再具体点"这种模糊层面，缺乏结构化的改进建议。
- **专业课复习低效**：保研面试的专业课提问覆盖面广，学生不知道哪些是高频考点、自己的薄弱点在哪里。
- **面试压力适应**：部分导师面试风格较为高压（快速打断、连续追问），学生如果没有提前适应，现场容易紧张失语。

### 核心痛点总结

> 保研面试的本质是"在压力下结构化表达自己的经历和知识"，而学生缺乏一个随时可用、能深入追问、能给结构化反馈的练习环境。

---

## 2. 产品设计说明

### 核心功能

| 功能 | 说明 |
|------|------|
| **保研面试模拟** | AI 扮演面试官（教授身份），围绕用户回答逐轮深入追问，模拟真实面试节奏 |
| **可调面试官属性** | 压力程度（友善/标准/严格）、专业方向、重点考察内容、面试轮次均可自定义 |
| **专业课快问快答** | 针对数据结构、机器学习、线性代数等科目，逐题出题+评分+参考答案 |
| **面试评分报告** | 面试结束后自动生成五维评分（内容深度、逻辑结构、表达清晰度、项目理解、应变能力）+ 改进建议 |
| **材料上传** | 支持上传简历/项目文档，AI 据此定向提问 |
| **面试记录** | 自动保存每次面试的完整对话，支持回看和对比 |
| **账号登录 + 云端存储** | 注册登录后记录跨设备同步，未登录也可正常使用（数据仅保留在当次会话） |

### 刻意没做的功能及原因

| 没做的功能 | 原因 |
|------------|------|
| **语音输入/输出** | 虽然真实面试是口语场景，但语音识别+合成会引入额外延迟和误差，且增加部署成本。文字模式下用户可以更专注于"内容组织"这一核心能力的训练。后续版本考虑。 |
| **第三方登录（微信/GitHub）** | 目标用户群体明确且规模有限，简单的账号密码足以满足需求，避免引入 OAuth 的复杂性和审核流程。 |
| **多人模拟/同伴匹配** | 社交功能的冷启动成本极高，且核心价值在于 AI 可以 7×24 随时练习，不依赖他人在线。 |
| **面试题库浏览** | 刻意不做静态题库页面。快问模式下题目是动态生成的，避免学生"背答案"而非真正理解。 |
| **付费/会员** | 当前阶段以验证产品价值为目标，不设使用限制。 |

### 设计决策

- **System prompt 以 user role 发送**：部分中转 API 对 system role 支持不一致，用 user role 承载系统指令更稳定。
- **前端重试 + 后端 fallback 双保险**：主 API 失败 3 次后自动切换备用 API，再加前端本地兜底回复，确保面试流程永不中断。
- **未登录也可使用**：降低使用门槛，登录仅用于云端保存记录。

---

## 3. 版本迭代记录

### v0.1 — 基础面试对话

**最初方案**：一个简单的聊天界面，用户输入回答，AI 按固定 prompt 追问。

**问题**：
- Prompt 过于简单，AI 容易变成"通用聊天"而非面试官
- 没有轮次控制，面试无法自然结束
- 没有设置项，所有人面对同一风格的面试官

### v0.2 — 面试官属性 + 快问模式

**改动**：
- 增加面试官属性设置（方向、压力、考察重点、轮次）
- 增加"专业课快问"模式，针对单科逐题训练
- Prompt 按模式和设置动态生成

**原因**：用户反馈"每次面试感觉都一样"，需要差异化场景。

### v0.3 — 评分报告 + 记录保存

**改动**：
- 面试结束后生成五维评分报告
- 面试记录保存到 localStorage
- 新增"面试记录"页面，支持回看

**问题**：
- localStorage 只在单设备有效
- 换浏览器/清缓存就丢失

### v0.4 — 登录系统 + 云端存储

**改动**：
- 后端增加 SQLite 数据库、JWT 鉴权、用户注册/登录
- 面试记录保存到服务器，跨设备可访问
- 初版做了全页面登录拦截

**问题**：
- 全页面登录拦截导致页面布局崩坏
- 未登录用户直接被挡在门外，违背"降低门槛"的原则

**修复**：
- 改为页面右上角登录按钮 + 弹窗模式
- 未登录也可正常使用所有功能
- 登录弹窗做成不透明实底背景，避免内容干扰

### v0.5 — 备用 API + 稳定性加固

**改动**：
- 后端 modelClient 增加备用 API（主 API 3 次失败后自动切换）
- 前端保留本地兜底回复作为最后防线

**原因**：主 API 偶尔不稳定/被限流，面试中途断线体验极差。

---

## 4. 下一步设计

如果再给一周时间，优先做以下内容：

### 高优先级

- **面试报告历史对比**：可视化展示多次面试的评分趋势，让用户看到进步轨迹
- **Prompt 优化 — 基于材料深度追问**：当前上传材料后 AI 只知道文件名，后续支持解析 PDF/文本内容，让追问更贴合用户真实项目

### 中优先级

- **面试记录导出**：支持导出为 PDF/Markdown，方便用户离线复习或分享给同学
- **更多专业课科目**：增加操作系统、计算机网络、数据库等保研高频科目的快问支持
- **移动端适配优化**：当前在手机上可用但体验欠佳，需优化触摸交互和布局

### 探索方向

- **语音模式**：集成 TTS + STT，模拟真实口语面试场景
- **面试官人格记忆**：让 AI 记住"这位用户上次面试暴露的弱点"，下次自动重点考察

---

## 5. AI 工具使用

| AI 工具 | 使用环节 | 具体用途 |
|---------|---------|---------|
| **Windsurf Cascade** | 全程开发 | 代码编写、架构设计、调试、部署——从前端组件到后端 API 到数据库 schema 均通过对话式协作完成 |
| **GPT-4.5 / GPT-5.5**（通过 API） | 产品核心功能 | 作为面试官/出题考官的推理引擎，生成追问、评分、报告 |
| **GPT 系列模型** | Prompt Engineering | 迭代测试不同 system prompt 的效果，调整面试官的追问策略和回复格式 |

### AI 在开发中的角色

本项目的开发过程本身就是"人 + AI 协作"的实践：

- **我（人）负责**：产品方向判断、用户需求定义、UI/交互决策、测试反馈
- **AI（Cascade）负责**：代码实现、技术方案选型、bug 定位与修复、部署脚本
- **协作模式**：我提出需求或描述问题 → AI 实现并解释 → 我验证效果并反馈 → AI 迭代修改

这种模式下，一个人可以在数天内完成从零到可用产品的全栈开发，验证了 AI 辅助开发在小型产品场景下的高效性。

---

## 附：技术栈

- **前端**：Next.js 16 + React + TypeScript
- **样式**：纯 CSS（自定义设计系统，深浅色主题）
- **后端**：Next.js App Router API Routes
- **数据库**：SQLite（better-sqlite3）
- **认证**：JWT（jose）+ bcryptjs + httpOnly Cookie
- **模型接口**：OpenAI 兼容格式（主 API + 备用 API 双链路）
- **部署**：阿里云 ECS + PM2 + Nginx
